name: bdd_showcase_app
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  # these lines added
  build_runner:
  bdd_widget_test: ^1.0.1
  golden_toolkit: ^0.9.0

flutter:
  uses-material-design: true
  assets:
    - images/